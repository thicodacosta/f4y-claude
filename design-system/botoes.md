# Botões

> Especificação completa em `components/Button.md` — este arquivo não duplica
> aquele conteúdo. Existe apenas para quem chega procurando por "botões" no
> nível raiz do Design System.

Ver `components/Button.md` para objetivo, quando usar/não usar, estados,
variações, exemplos, código e acessibilidade.
