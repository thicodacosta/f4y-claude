# Automation — LinkedIn

Não implementado. A skill `linkedin` cobre hoje apenas a criação de conteúdo
(posts/artigos), não a publicação automática via API.

`[TODO] Definir com Thiago` — se/quando a publicação automática for
necessária, o fluxo (setup, credenciais, publicação, troubleshooting) deve
nascer na skill `linkedin` (seguindo o padrão já usado na skill `instagram`),
e esta pasta passa a guardar só script e `.env.example`.
