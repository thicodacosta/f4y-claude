# Automation — CRM

Não implementado. Nenhum CRM está integrado a este workspace hoje —
`docs/clientes/` é, por ora, a única base de registro de cliente, mantida
manualmente.

`[TODO] Definir com Thiago` — qual CRM (se algum) será adotado e como se
relaciona com `docs/clientes/` (substitui, sincroniza, ou `docs/clientes/`
continua sendo a fonte consultada pelas skills). Quando construída, o
procedimento nasce em uma skill (nova ou existente), não só nesta pasta.
